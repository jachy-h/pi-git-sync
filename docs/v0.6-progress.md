# v0.6 实施进度

> 对应方案：[`v0.6-plan.md`](./v0.6-plan.md)  
> 当前状态：Phase 1 源码组织调整已完成；Phase 2 尚未开始  
> 目标版本：`0.6.0`  
> 代码基线：`0.5.1`  
> 更新时间：2026-07-30

## 总体进度

| 阶段 | 状态 | 说明 |
| --- | --- | --- |
| Phase 0：方案确认与基线 | 已完成 | 确认浅层职责目录方案，建立当前测试、coverage 与复杂度基线。 |
| Phase 1：纯组织调整 | 已完成 | 已移动 24 个生产模块、更新显式 import 与 coverage gate；未改变同步行为。 |
| Phase 2：结构化结果与低风险去重 | 未开始 | Phase 1 独立提交后开始。 |
| Phase 3：pull / push / conflict flow 收敛 | 未开始 | 不与文件移动混合。 |
| Phase 4：setup 与可见性收敛 | 未开始 | 不与文件移动混合。 |
| Phase 5：验证与文档 | 未开始 | 最终发布验证。 |

## 已完成：Phase 0 — 方案确认与基线

### 目录决策

- [x] 确认使用 `orchestration / sync / system / extension` 四个浅层职责目录。
- [x] `index.ts` 保持项目根目录，作为 Pi package 入口。
- [x] 保持显式 import，不引入 barrel files。
- [x] 确认本轮文件移动与行为重构、测试目录移动分离。
- [x] 确认高层 conflict coordination 将在后续提取为 `orchestration/conflict-flow.ts`；现有低层 Git conflict transaction 迁入 `system/conflict-resolution.ts`。

### 源码映射（Phase 1）

| 当前文件 | Phase 1 目标文件 | 原因 |
| --- | --- | --- |
| `src/commands.ts` | `src/orchestration/commands.ts` | 公共 façade、lifecycle、锁与顶层编排。 |
| `src/operation-context.ts` | `src/orchestration/operation-context.ts` | 操作上下文类型。 |
| `src/operation-result.ts` | `src/orchestration/operation-result.ts` | 结构化命令结果与冲突请求。 |
| `src/setup-flow.ts` | `src/orchestration/setup-flow.ts` | setup 业务流程。 |
| `src/pull-phase.ts` | `src/orchestration/pull-phase.ts` | pull 的无锁阶段。 |
| `src/push-phase.ts` | `src/orchestration/push-phase.ts` | push 的无锁阶段。 |
| `src/apply-transaction.ts` | `src/orchestration/apply-transaction.ts` | apply 的提交与回滚边界。 |
| `src/config.ts` | `src/sync/config.ts` | 同步清单。 |
| `src/glob.ts` | `src/sync/glob.ts` | 同步路径匹配与规范化。 |
| `src/inventory.ts` | `src/sync/inventory.ts` | 三方比较。 |
| `src/capture.ts` | `src/sync/capture.ts` | agent → repo 文件镜像。 |
| `src/materialize.ts` | `src/sync/materialize.ts` | repo → agent 文件镜像。 |
| `src/settings-portability.ts` | `src/sync/settings-portability.ts` | settings 可移植性校验。 |
| `src/validate.ts` | `src/sync/validate.ts` | 同步内容校验。 |
| `src/git.ts` | `src/system/git.ts` | Git subprocess 边界。 |
| `src/state.ts` | `src/system/state.ts` | 本机同步基线持久化。 |
| `src/backup.ts` | `src/system/backup.ts` | 备份与恢复副作用。 |
| `src/lock.ts` | `src/system/lock.ts` | 跨进程同步锁。 |
| `src/packages.ts` | `src/system/packages.ts` | package 安装、审批与回滚。 |
| `src/path-safety.ts` | `src/system/path-safety.ts` | 本机路径 / symlink 边界。 |
| `src/security.ts` | `src/system/security.ts` | hard deny 与 secret scan。 |
| `src/conflict-resolution.ts` | `src/system/conflict-resolution.ts` | 低层 Git conflict transaction。 |
| `src/operation-runner.ts` | `src/extension/operation-runner.ts` | Pi 无关的 UI 操作运行器。 |
| `src/ui.ts` | `src/extension/ui.ts` | Pi/TUI 格式化适配。 |

`orchestration/conflict-flow.ts` 不在 Phase 1 创建；它属于 Phase 3 的职责提取，避免在“纯移动”提交中新增或迁移业务逻辑。

### 验证基线

| 检查 | 结果 |
| --- | --- |
| `npm run typecheck` | 通过 |
| core coverage | 33 files / 339 tests 通过；statements/lines 80.55%、branches 81.85%、functions 92.60% |
| E2E | 1 file / 4 tests 通过 |
| `commands.ts` coverage | statements/lines 69.33%、branches 73.90%、functions 92.18% |
| 生产依赖循环 | 0（现有 review graph 基线） |

## 已完成：Phase 1 — 纯组织调整

- [x] 建立 `src/orchestration/`、`src/sync/`、`src/system/`、`src/extension/` 并移动 24 个生产模块。
- [x] 更新 `index.ts`、生产模块与测试中的全部显式相对 import。
- [x] 检查确认旧平铺 `src/*.ts` import 为 0，缺失的相对 TypeScript import 为 0。
- [x] 将 coverage gate 的 `commands.ts` 与 `operation-runner.ts` 路径更新为新位置；阈值未改变。
- [x] 更新 `docs/architecture.md` 的文件树和模块图；文档版本标记为 `0.6.0-dev`。
- [x] 执行 typecheck、core coverage、E2E、LSP 与机械 diff 审查。
- [x] 作为独立的纯移动提交提交；不混入 Phase 2 行为重构。

### 验证记录

| 日期 | 检查 | 结果 |
| --- | --- | --- |
| 2026-07-30 | `npm run test:ci`（移动前） | 通过：typecheck；core 33 files / 339 tests；E2E 1 file / 4 tests。 |
| 2026-07-30 | moved import audit | 旧平铺 `src/*.ts` 引用 0；缺失的相对 `.ts` 引用 0。 |
| 2026-07-30 | LSP | `index.ts`、24 个 `src/` 文件和 40 个 `test/` 文件无 TypeScript diagnostics。 |
| 2026-07-30 | `npm run test:ci`（移动后） | 通过：typecheck；core 33 files / 339 tests，80.55% statements/lines、81.84% branches、92.60% functions；coverage gate 通过；E2E 1 file / 4 tests。 |
| 2026-07-30 | mechanical diff audit | 除 coverage gate 路径/格式调整外，301 行源码与测试差异均为 TypeScript 模块路径更新。 |

### 已知后续项

- 辅助静态规则报告的既有复杂度与样式提示（如 `commands.ts` 大类、嵌套条件和 non-null assertion）不在纯移动范围内；其中编排层复杂度将由 Phase 2–4 处理。
- `test/` 继续保持平铺，待生产边界稳定后再按目录逐步整理。

## 进度更新规则

1. 每完成一个阶段项立即更新本文件；
2. 验证记录必须写明命令、日期和结果；
3. 测试失败或重构不完整时，阶段保持“进行中”；
4. 行为改变或架构决策调整必须同步更新 `v0.6-plan.md`；
5. 文件移动与行为重构必须使用独立提交。
