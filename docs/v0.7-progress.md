# v0.7 实施进度

> 对应方案：[`v0.7-plan.md`](./v0.7-plan.md)  
> 当前状态：P0 已完成；后续 P1/P2 与发布验证待执行  
> 目标版本：`0.7.0`  
> 当前代码基线：`0.6.3`  
> 更新时间：2026-08-04

## 总体进度

| 阶段 | 状态 | 说明 |
| --- | --- | --- |
| Phase 0：基线与体验规格 | 已完成 | 建立同步计划、冲突选择与 coverage gate 基线；完整 CI 已通过。 |
| Phase 1：只读可见性 | 未开始 | 状态面板、共享计划摘要和设置预检。 |
| Phase 2：计划确认与安全执行 | 已完成 | read-only plan、确认界面与执行前 fingerprint 校验已实现并经完整 CI 验证。 |
| Phase 3：冲突与范围管理 | 进行中 | P0 逐路径本机/远端选择已完成；P1 范围管理未开始。 |
| Phase 4：历史、文档与发布 | 未开始 | 操作历史、发布校验、pack 和跨平台验证。 |

## P0：同步计划确认

- [x] 新增 `PiSyncCommands.plan()`，以三方比较、Git ahead/behind、package diff 和 pending operation 生成只读计划。
- [x] 为 ready plan 生成 SHA-256 fingerprint；`run()` 在取得同步锁后重建计划并拒绝过期确认。
- [x] 在 `/pisync` 的高影响同步前展示文件、远端提交、package 和 recovery 摘要。
- [x] 用户拒绝计划时不启动同步；无交互 UI 时 fail-safe。
- [x] 完整 CI 与两设备 E2E 验证；跨平台 smoke 留待 Phase 4 发布验证。

## P0：文件级冲突决策

- [x] 新增 `ConflictPathChoices`，并只接受当前 `SyncConflictRequest` 中全部路径的本机/远端选择。
- [x] TUI 支持逐路径选择，展示路径与 change type，并在执行前回显决策摘要。
- [x] 冲突 resolver 在 Git merge 后验证实际未合并路径仍与选择匹配；路径变化会安全阻断。
- [x] 保留现有一键“全部使用本机 / 远端”、Agent handoff、手动合并和恢复分支。
- [x] 完整 CI 与两设备 E2E 验证；跨平台 smoke 留待 Phase 4 发布验证。

## 覆盖率门禁

- [x] 定位失败：`src/orchestration/commands.ts` functions 为 88.73%，低于 89% 阈值，导致 `npm run test:ci` 在 E2E 前中止。
- [x] 新增 fingerprint plan 过期拒绝测试，以及计划拒绝和逐路径选择的 extension 测试。
- [x] 执行 coverage suite，`commands.ts` functions 为 89.18%，满足 89% 门禁。
- [x] 执行完整 `npm run test:ci`，372 个 core tests 与 4 个两设备 E2E tests 均通过。

## 验证记录

| 日期 | 检查 | 结果 |
| --- | --- | --- |
| 2026-08-04 | 审查前 `npm run test:ci` | 369 个测试通过；coverage gate 失败：`src/orchestration/commands.ts` functions 88.73% < 89%；E2E 未执行。 |
| 2026-08-04 | P0 targeted suite | `commands-run`、`conflict-resolution`、`extension-manual-merge`：3 files / 34 tests 通过。 |
| 2026-08-04 | extension suite | `test/extension.test.ts`：33 tests 通过。 |
| 2026-08-04 | `npm run test:coverage` | 37 files / 372 tests 通过；总 functions 93.69%；`commands.ts` functions 89.18%；coverage gate 通过。 |
| 2026-08-04 | `npm run test:ci` | typecheck、coverage gate 与 1 file / 4 tests 的两设备 E2E 全部通过。 |

## 进度更新规则

1. 每完成一个任务立即更新本文件和对应 plan；
2. 验证记录必须写明命令、日期和结果；
3. 测试失败或实现不完整时，阶段保持“进行中”；
4. 行为或安全边界变化必须同步更新 `v0.7-plan.md`；
5. 发布前必须通过 `npm run test:ci`、跨平台 smoke 和 package dry-run。
